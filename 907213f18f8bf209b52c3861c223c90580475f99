{"verdict":"changes","notes":"mergeVerified writes refs/forest/merged/\u003cid\u003e before projectMerge, while reconcileMerged treats any such ref as proof that the merge landed. A crash, or an ambiguous host/network error, after markMerged but before or during projectMerge can therefore make restart close the item and delete its branch; the error path's dropMerged can also delete a valid pre-existing fact or erase proof after a merge actually succeeded. The retry/idempotency contract is only changed in memoryTracker: githubTracker.Close remains a raw gh issue close, and cleanup operations can return CAS errors under concurrent retries. Use a durable pending/landed state or an atomic/observable host-merge protocol, and make the real Tracker and cleanup effects idempotent before approving.","reviewer":"verifier","model":"openrouter/openai/gpt-5.6-luna","def_sha":"8531e5c611fd2ed2","run_id":"20260808T084139Z-branch-forest/190-make-merge-finalisation-reconcil","time":"2026-08-08T08:44:11Z"}

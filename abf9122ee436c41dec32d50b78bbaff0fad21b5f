{"verdict":"changes","notes":"In settleNoteWrite, the !durable branch pushes the already-existing local note and returns nil on success. Since git notes add failed with errNoteExists, the value passed to writeNote was not installed; callers such as verifierReview and the checks gate then continue using their newly computed in-memory verdict/checks while the remote contains the older local note. After a successful retry, return the winner signal (errNoteExists) so callers reread the durable note, and add a regression test for a pre-existing local note with a different proposed value.","reviewer":"verifier","model":"openrouter/openai/gpt-5.6-luna","def_sha":"8531e5c611fd2ed2","run_id":"20260808T053409Z-branch-forest/189-never-read-a-local-note-that-fai","time":"2026-08-08T05:39:18Z"}

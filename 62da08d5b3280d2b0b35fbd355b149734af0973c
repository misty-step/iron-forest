{"verdict":"changes","notes":"`issueData` and `reviewData` now expose only `ID`, while `agents/builder/prompt.md:1` and `agents/verifier/prompt.md:1` still render `{{.Number}}`; Go templates will therefore give the Builder/Fixer and Verifier an empty/missing item identity. Because `agents/` is protected, the required operator follow-up must be stated exactly: change `{{.Number}}` to `{{.ID}}` on line 1 of both prompt files (the Builder prompt also serves the Fixer). Alternatively, retain a `Number` compatibility key while exposing `ID`. In addition, `encodeBranchID`/`itemIDFromBranch` do not round-trip an id containing the literal sequence `%2D` (for example `%2D` encodes to `%252D`, then decodes to `-`); either make the encoding/decoding genuinely bijective and add a regression test, or do not claim that any opaque id round-trips.","reviewer":"verifier","model":"openrouter/openai/gpt-5.6-luna","def_sha":"2ffb54e0fa1d16be","run_id":"20260807T152017Z-branch-forest/149-migrate-item-identity-to-opaque","time":"2026-08-07T15:21:54Z"}

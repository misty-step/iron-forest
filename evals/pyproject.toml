[project]
name = "iron-forest-evals"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "harbor==0.21.0",
]

[project.optional-dependencies]
langfuse = [
  "langfuse==4.14.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["iron_forest_eval"]

# Go build output
/forest
/forest.next
# Prior binaries created this file. New daemons remove it during startup.
/forest.prev
/dist/
*.test

# forest workspace (per-run worktrees, ledger, traces)
/.forest/
/.omp/

# editor
.DS_Store
/iron-forest

# one-off published snapshots, not repository artifacts
/docs/*.html

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "manager report",
  "description": "Output contract for report.json from the Manager lane.",
  "type": "object",
  "required": ["pick"],
  "properties": {
    "pick": {
      "type": "string",
      "description": "the id of the one chosen candidate"
    }
  }
}

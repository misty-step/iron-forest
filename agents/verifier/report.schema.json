{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "verifier verdict",
  "description": "Output contract for review.json from the Verifier lane.",
  "type": "object",
  "required": ["verdict", "summary"],
  "properties": {
    "verdict": {
      "type": "string",
      "enum": ["approve", "changes"]
    },
    "summary": {
      "type": "string",
      "description": "one or two sentences on the review"
    },
    "notes": {
      "type": "string",
      "description": "specific feedback for the Builder, or \"none\""
    }
  }
}

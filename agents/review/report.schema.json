{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "review verdict",
  "description": "Output contract for the review agent's review.json. The review gate rejects a run whose review.json does not satisfy it.",
  "type": "object",
  "required": ["verdict", "summary"],
  "properties": {
    "verdict": {
      "type": "string",
      "enum": ["approve", "changes"]
    },
    "summary": {
      "type": "string",
      "description": "one or two sentences on the review"
    },
    "notes": {
      "type": "string",
      "description": "specific feedback for the author, or \"none\""
    }
  }
}

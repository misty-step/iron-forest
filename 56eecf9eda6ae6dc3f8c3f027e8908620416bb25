{"verdict":"changes","notes":"The pre-change command preserves the distinction between a checks note whose `results` field is explicitly `[]` and one whose field is null/omitted: unmarshalling followed by json.MarshalIndent emits `[]` for the former. `core.Notes` and `cmdShow` collapse every zero-length result slice to nil, so an explicit empty array is now rendered as `results: null`. Preserve the nil/non-nil shape through the core API and add a regression test for an explicit empty array before merging.","reviewer":"verifier","model":"openrouter/openai/gpt-5.6-luna","def_sha":"03143758ed90c616","run_id":"20260807T223625Z-branch-forest/176-route-every-read-command-through","time":"2026-08-07T22:39:31Z"}

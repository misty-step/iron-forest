{"verdict":"changes","notes":"`coreImpl.Notes` now preserves a nil checks-results slice and `cmdShow` serializes it as `null`; on the base revision, `cmdShow` always constructed a non-nil zero-length Results slice, so a checks note with `results: null` (for example `writeChecks(..., checksNote{Status: \"pass\"})`) emitted `\"results\": []`. Preserve the old normalization while keeping explicit empty arrays as arrays. Also, setting `Present=true` for every decoded note bypasses the old `Time != \"\" || RunID != \"\"` presence heuristic, so notes lacking both fields are newly emitted; add base-output regression coverage and retain the old behavior.","reviewer":"verifier","model":"openrouter/openai/gpt-5.6-luna","def_sha":"03143758ed90c616","run_id":"20260807T224950Z-branch-forest/176-route-every-read-command-through","time":"2026-08-07T22:52:42Z"}

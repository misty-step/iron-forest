{"verdict":"changes","notes":"1. `projection.go:112-140` only lists the pull request and then invokes `gh pr merge`; it does not pass an atomic expected-head condition, so a push between those calls can make the host merge an unchecked revision. The check also accepts an empty `headRefOid` (`HeadSHA != \"\" \u0026\u0026 ...`). Require a non-empty matching SHA and a provider-side compare-and-swap such as the host merge API's expected-head option. 2. `verifierFlow.Select` and `Act` omit the durable attempt count from the FSM facts for bare, checks-recorded, and rejected heads; only the approved merge path reads it. If the Fixer bumps the attempt ref and crashes or fails before applying `forest:failed`, the Verifier still acts on the exhausted subject and can write Checks/Verdict notes, contrary to the documented terminal state and the claim that a post-bump crash is observed as `failed`. Feed the cap facts through every selector/effect boundary and preserve the human halt. 3. `assertNotFailed` is checked only before merge. A `forest:failed` label arriving while checks or review is running is not revalidated before `writeChecks`/`writeVerdict` (the mid-pass test only proves that merge is stopped), so a terminal subject can still receive new durable decisions. Revalidate at each durable effect boundary or narrow the stated invariant. 4. `AGENTS.md:35-42` still says there is no protected-path check and that agents may change `forest.yaml` and `agents/`, while `gate.go`, ADR 0004, `docs/fsm.md`, and README enforce the opposite policy and explicitly link AGENTS. Update the contributor contract so the repository has one source of truth.","reviewer":"verifier","model":"openrouter/openai/gpt-5.6-luna","def_sha":"8519c03d6822e6ba","run_id":"20260807T214448Z-branch-forest/171-document-and-enforce-the-deliver","time":"2026-08-07T21:48:30Z"}

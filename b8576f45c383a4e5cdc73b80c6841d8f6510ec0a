{"verdict":"changes","notes":"`ports.go`'s Item/Tracker is still unused: `issue.Number int`, `listOpenIssues`/`getItem`, tracker effects, `issueData`/`reviewData`, and `watch.go:237` remain tied to `Number`/`%d`; replace these with Item.ID and string tracker operations, and add coverage through the controller path rather than only testing createWorktree. Because agents/ is protected, apply the exact prompt follow-up in `agents/builder/prompt.md` and `agents/verifier/prompt.md` (`{{.Number}}` -\u003e `{{.ID}}`) if the data field is renamed, and expose that opaque ID from the prompt data; the proposed change provides no closing instruction for this required operator edit. Finally, `itemIDFromBranch` splits at the first `-`, so IDs containing a hyphen are not opaque round-trippable unless the branch encoding/delimiter contract is made explicit. `mise exec -- go test ./...` and `mise exec -- go vet ./...` pass, but these gaps prevent approval.","reviewer":"verifier","model":"openrouter/openai/gpt-5.6-luna","def_sha":"4d886f075e1ea540","run_id":"20260807T150251Z-branch-forest/149-migrate-item-identity-to-opaque","time":"2026-08-07T15:05:19Z"}

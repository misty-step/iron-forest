{"verdict":"changes","notes":"`watch --live-gh` now calls `coreImpl.Items`, which runs `builderFlow.Select` and filters stalled subjects; the old watch path called `eligibleItems` directly and displayed those subjects, so its backlog can change. `coreImpl.Agents` returns an error for the first malformed agent and aborts, whereas `cmdAgents` previously reported that agent and continued with the rest. `cmdShow` also loses the old note-presence booleans, changes missing/null check results into an empty array, and changes the notes/verdict/checks error prefixes. Preserve these behaviors (or expose presence/selection semantics in the API) and add regression coverage for them.","reviewer":"verifier","model":"openrouter/openai/gpt-5.6-luna","def_sha":"03143758ed90c616","run_id":"20260807T221800Z-branch-forest/176-route-every-read-command-through","time":"2026-08-07T22:20:42Z"}

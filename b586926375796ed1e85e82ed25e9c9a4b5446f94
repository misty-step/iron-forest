{"verdict":"changes","notes":"`transit` and `observe` are never called by any Selector or Act path; the existing flows continue to run checks, write notes, publish, and merge directly. Consequently the tests only validate a synthetic helper and would still pass if a Flow skipped a required note or Gate. Integrate the FSM with the actual effect boundaries and add flow-level illegal-transition coverage. The item requires Gate rejection of protected paths (`.forest/`, `forest.yaml`, `agents/`, `.opencode/opencode.json`, etc.), but `gate.go` explicitly has no such check and docs/fsm.md declares that invariant superseded; restore the enforcement and tests. Also correct the FSM semantics: `observe` returns verdict_approved/verdict_rejected even when Checks are failing, while `transit` requires the Verdict outcome to already be present before the review effect that writes it, and no fact carries exact Revision/actor identity to enforce the required exact-note and ownership invariants.","reviewer":"verifier","model":"openrouter/openai/gpt-5.6-luna","def_sha":"8519c03d6822e6ba","run_id":"20260807T182800Z-branch-forest/171-document-and-enforce-the-deliver","time":"2026-08-07T18:29:08Z"}
